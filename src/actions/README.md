# src/actions

Reserved for write hooks (MVP 2: transfers, MVP 3: swaps), e.g. `useSendToken`, `useSwap`.

MVP 1 is read-only: this folder must stay empty of code. All read-only data hooks live in `src/data/`.
